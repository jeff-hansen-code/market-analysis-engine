name: .NET API CI

on:
  push:
    paths:
      - "backend/api/**"
      - ".github/workflows/dotnet-api-ci.yml"
  pull_request:
    paths:
      - "backend/api/**"
      - ".github/workflows/dotnet-api-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend/api/MarketAnalysis.Api

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      # keep this even if no tests yet; will just be a quick no-op later
      - name: Test
        run: dotnet test || echo "No tests yet"
